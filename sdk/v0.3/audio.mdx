---
title: "pixeltable.functions.audio"
sidebarTitle: "audio"
icon: "square-m"
---

Pixeltable [UDFs](https://pixeltable.readme.io/docs/user-defined-functions-udfs) for `AudioType`.

Example:

``` python
import pixeltable as pxt
import pixeltable.functions as pxtf

t = pxt.get_table(...)
t.select(pxtf.audio.get_metadata()).collect()
```


<a href="https://github.com/pixeltable/pixeltable/blob/v0.3/pixeltable/functions/audio.py#L0" target="_blank">View source on GitHub</a>

## UDFs


---

### `get_metadata()` <sub>udf</sub>

Gets various metadata associated with an audio file and returns it as a dictionary.


**Signature:**

```python
get_metadata(audio: Audio)-> Json
```

