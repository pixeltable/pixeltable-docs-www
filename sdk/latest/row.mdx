---
title: "Row SDK reference"
icon: "square-c"
description: "API reference for the Pixeltable Row type returned by query results, providing dict-like access to column values for each row of a table or view."
---
# <span style={{ 'color': 'gray' }}>class</span>&nbsp; pixeltable.Row


A dict-like wrapper over a single result row.

Supports key access (`row['col']`), membership (`'col' in row`),
iteration over keys, and the standard `get`, `keys`, `values`,
and `items` methods.

