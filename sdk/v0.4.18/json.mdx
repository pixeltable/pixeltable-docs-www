---
title: "pixeltable.functions.json"
sidebarTitle: "json"
icon: "square-m"
---

Pixeltable [UDFs](https://pixeltable.readme.io/docs/user-defined-functions-udfs) for `JsonType`.

Example:

``` python
import pixeltable as pxt
import pixeltable.functions as pxtf

t = pxt.get_table(...)
t.select(pxtf.json.make_list(t.json_col)).collect()
```


<a href="https://github.com/pixeltable/pixeltable/blob/v0.4.18/pixeltable/functions/json.py#L0" target="_blank">View source on GitHub</a>

## UDFs


---

### `make_list()` <sub>udf</sub>

Collects arguments into a list.


**Signature:**

```python
make_list(obj: Json)-> Json
```

